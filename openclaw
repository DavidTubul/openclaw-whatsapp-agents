#!/bin/bash
# wrapper: ensures Node 22 (required by openclaw) before invoking the CLI
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm use 22 > /dev/null 2>&1
exec openclaw "$@"
